[
      {
        "text": "<%= strip_ticks(@quote.quote) %><br><span style='text-align:left;font-size: 9px'><%= strip_ticks(@quote.author) %> <%= strip_ticks(@quote.book) %> <%= strip_ticks(@quote.citation) %></span>",
        "favorite": "<%= @quote.favorite ? @quote.favorite : 'n' %>",
        "id": "<%= @quote.id %>",
		"topic": "<%=@topic_name.capitalize %>",
		"image_url": "<%=@image_url%>"
      },
        
      <% @quotes.each do |quote| %>        
         { "text": "<%= strip_ticks(quote.quote) %><br><span style='text-align:left;font-size: 9px'><%= strip_ticks(quote.author) %> <%= strip_ticks(quote.book) %> <%= strip_ticks(quote.citation) %> </span>",
        "favorite": "<%= quote.favorite ? quote.favorite : 'n' %>",        
         "id": "<%= quote.id %>"
      }<% unless @quotes.last == quote %>,<% end %>
        <% end %>    
]